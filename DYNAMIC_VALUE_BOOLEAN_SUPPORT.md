# 动态数值布尔类型支持

## 概述

为动态数值系统添加了布尔表达式支持，使得包含比较运算符的公式（如 `生命值 > 50`）能够被识别为布尔类型，并在条件编辑器中正确显示和使用。

## 修改的文件

### 1. FormulaEvaluator.swift

**新增功能：**

- **`isBooleanExpression(_ expression: String?) -> Bool`**
  - 判断公式是否为布尔表达式（包含顶层比较运算符）
  - 检测 `==`, `!=`, `>`, `>=`, `<`, `<=` 运算符
  - 忽略括号内的比较运算符

**增强的解析器：**

- 添加了 `parseComparison()` 方法来处理比较运算符
- 将原有的 `parseExpression()` 重命名为 `parseAdditive()`
- 新增辅助方法：
  - `matchString(_ expected: String) -> Bool` - 匹配多字符运算符
  - `peekEquals() -> Bool` - 前瞻检查等号

**支持的比较运算符：**

- `==` - 等于
- `!=` - 不等于
- `>` - 大于
- `>=` - 大于等于
- `<` - 小于
- `<=` - 小于等于

**求值结果：**

- 比较结果为真时返回 `1.0`
- 比较结果为假时返回 `0.0`
- 整数化后为 `1` 或 `0`

### 2. ModuleEditorSupport.swift

**修改 `conditionFields()` 方法：**

```swift
for name in adjustmentTargetFields(module: module) {
    // 检查该动态数值是否为布尔表达式
    let isBool = module.valueAdjustments.first { 
        $0.field == name && FormulaEvaluator.isBooleanExpression($0.formula) 
    } != nil
    let fieldType: ConditionFieldType = isBool ? .bool : .int
    add(ConditionField(name: name, displayName: "\(name) (动态数值)", 
                      type: fieldType, category: .dynamicValue))
}
```

这个修改会：
- 检查每个动态数值的公式是否为布尔表达式
- 如果是布尔表达式，将字段类型设置为 `.bool`
- 如果不是，保持原有的 `.int` 类型

### 3. ConditionEditorSheet.swift

**无需修改** - 现有代码已经正确处理布尔类型字段：

- 第 285 行：布尔类型字段只使用 `==` 和 `!=` 运算符
- 第 310-314 行：布尔类型字段显示为下拉选择框（是/否）

## 使用示例

### 1. 创建布尔类型的动态数值

在"公式动态数值"中添加：

```
测试生命 = 生命值 > 50
低血量 = 生命值 < 30
满法力 = 法力值 == 100
高血量高法力 = 生命值 > 80 && 法力值 > 80
```

### 2. 在条件编辑中使用

当在条件编辑器中选择这些动态数值时：

- **类型**: 会显示为"动态数值"
- **判断**: 只显示 `==` 和 `!=` 两个选项（而不是全部运算符）
- **值**: 显示为下拉框，选项为"是 (true)"或"否 (false)"

### 3. 条件预览

```
测试生命 == true   # 当生命值 > 50 时为真
低血量 == false    # 当生命值 >= 30 时为假
```

## 技术细节

### 布尔表达式检测逻辑

1. 解析表达式，跟踪括号深度
2. 只在顶层（depth == 0）检查比较运算符
3. 双字符运算符（`==`, `!=`, `>=`, `<=`）优先检查
4. 单字符运算符（`>`, `<`）确保不是双字符运算符的一部分

### 求值顺序

```
parseComparison()
  ↓
parseAdditive() (原 parseExpression)
  ↓
parseTerm()
  ↓
parseFactor()
  ↓
parsePrimary()
```

比较运算符具有最低的优先级，在加法/减法之后求值。

### 运算符优先级

从高到低：
1. 函数调用、括号、字面量、字段引用
2. 一元 `+` 和 `-`
3. `*` 和 `/`
4. `+` 和 `-`
5. 比较运算符 `==`, `!=`, `>`, `>=`, `<`, `<=`

## 测试建议

1. **基本比较**
   - 测试所有六种比较运算符
   - 测试与常量和字段的比较

2. **复杂表达式**
   - `(生命值 + 法力值) > 150`
   - `max(生命值, 法力值) >= 100`
   - `生命值 * 2 > 法力值`

3. **边界情况**
   - 相等情况：`50 == 50` → true
   - 空值处理
   - 除零保护（已有）

4. **UI 集成**
   - 确认条件编辑器正确识别布尔类型
   - 确认运算符下拉只显示 `==` 和 `!=`
   - 确认值下拉显示"是/否"选项

## 后续增强建议

1. **逻辑运算符支持**
   - 添加 `&&` (与) 和 `||` (或) 运算符
   - 添加 `!` (非) 运算符

2. **三元运算符**
   - 支持 `条件 ? 值1 : 值2` 语法

3. **字符串比较**
   - 支持字符串相等性比较
   - 支持字符串包含检查

4. **范围检查**
   - 添加 `between(值, 最小, 最大)` 函数
   - 添加 `in_range` 运算符

## 注意事项

1. **向后兼容性**
   - 现有的数值公式不受影响
   - 只有包含比较运算符的公式才会被识别为布尔类型

2. **性能**
   - `isBooleanExpression()` 对每个动态数值只调用一次
   - 检测算法时间复杂度为 O(n)，n 为公式长度

3. **错误处理**
   - 保持现有的错误消息和处理逻辑
   - 比较运算符的语法错误会正常报告

## 总结

这次修改实现了动态数值的布尔类型支持，使得用户可以：

1. 在动态数值公式中使用比较运算符
2. 系统自动识别布尔表达式
3. 在条件编辑器中正确使用布尔类型的动态数值
4. 获得更直观的用户体验（下拉选择是/否而不是输入0/1）

所有修改都经过编译验证，并且保持了与现有代码的兼容性。
